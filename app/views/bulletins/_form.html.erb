<%= simple_form_for(@bulletin) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description, input_html: { rows: 10, cols:100} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
